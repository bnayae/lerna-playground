{
  "name": "root",
  "private": true,
  "scripts": {
    "dev":"lerna run dev",
    "dev:a":"lerna run dev --scope=@lerna-playground/app-a",
    "dev:b":"lerna run dev --scope=@lerna-playground/app-b",
    "start":"lerna run start",
    "test":"lerna run test",
    "diff": "lerna diff",
    "new-version":"lerna version --conventional-commit --yes",
    "lerna:update": "lernaupdate",
    "deploy:production": "now && now alias"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "lerna-update-wizard": "^0.17.8"
  }
}
